import { Qn as toRef, Rn as isRef, U as computed } from "./vue.runtime.esm-bundler-DJa8Dn_V.js";
import { c as getCurrentInstanceName, l as propsFactory } from "./defineComponent-DkOniQAC.js";
//#region node_modules/vuetify/lib/composables/border.js
var makeBorderProps = propsFactory({ border: [
	Boolean,
	Number,
	String
] }, "border");
function useBorder(props, name = getCurrentInstanceName()) {
	return { borderClasses: computed(() => {
		const border = props.border;
		if (border === true || border === "") return `${name}--border`;
		else if (typeof border === "string" || border === 0) return String(border).split(" ").map((v) => `border-${v}`);
		return [];
	}) };
}
//#endregion
//#region node_modules/vuetify/lib/composables/elevation.js
var makeElevationProps = propsFactory({
	elevation: {
		type: [Number, String],
		validator: (value) => parseInt(value) >= 0
	},
	hoverElevation: {
		type: [Number, String],
		validator: (value) => parseInt(value) >= 0
	}
}, "elevation");
function useElevation(props) {
	return { elevationClasses: toRef(() => {
		const elevation = isRef(props) ? props.value : props.elevation;
		const hoverElevation = isRef(props) ? null : props.hoverElevation;
		if (elevation == null) return [];
		return [...elevation == null ? [] : [`elevation-${parseInt(elevation)}`], ...hoverElevation == null ? [] : [`hover-elevation-${parseInt(hoverElevation)}`]];
	}) };
}
//#endregion
export { useBorder as i, useElevation as n, makeBorderProps as r, makeElevationProps as t };
